Разработать приложение для управления пользователями (CRUD)

    1. Наличие RESTful интерфейса, через который можно будет взаимодействовать с
    приложением через клиент (Например: Postman);
    2. Поддержка операций управления (создания, чтения, редактирования и удаления) пользователями.
    Пользователь определяется следующими значениями;
        -Имя
        -Фамилия
        -Дата рождения

Нефункциональные требования:
    -Система хранения данных в XML файле или в базе данных;
    -Приложение должно собираться в maven без установки и настройки каких либо дополнительных компонент;